import React from "react";
import { StyledHeader, Title } from "./StyledHeader";

const Header = () => {
  return (
    <StyledHeader>
      <Title>GoDaddy's Repositories</Title>
    </StyledHeader>
  );
};

export default Header;
