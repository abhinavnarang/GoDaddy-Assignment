import React from "react";
import { StyledHeader, Title } from "./StyledHeader"; // Import styled components

const Header = () => {
  return (
    <StyledHeader>
      <Title>GoDaddy's Repositories</Title>
    </StyledHeader>
  );
};

export default Header;
